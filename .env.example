# Alchemy Encryption Password
# This is used to encrypt secrets in Alchemy's state
# Generate a strong random password and keep it secure
ALCHEMY_PASSWORD=your-secure-random-password-here

# API Authentication Token
# Used to protect the /backups and /backup endpoints
# Generate a secure random token (e.g., openssl rand -base64 32)
API_TOKEN=your-secure-api-token-here

# NeonDB Connection
# Get from: https://console.neon.tech/ → Your Project → Connection String
NEON_DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require
